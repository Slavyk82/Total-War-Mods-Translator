// TODO(task-13): replace this placeholder with the full progress modal.
import 'package:flutter/material.dart';

class BulkOperationProgressDialog extends StatelessWidget {
  const BulkOperationProgressDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const AlertDialog(
      title: Text('Bulk operation'),
      content: Text('Placeholder — replaced in Task 13.'),
    );
  }
}
